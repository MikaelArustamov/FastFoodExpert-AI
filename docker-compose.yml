version: '3.8'

services:
  potato-expert:
    build: .
    volumes:
      - ./potato_db:/app/potato_db
      - ./chat.log:/app/chat.log
      - ./security.log:/app/security.log
    network_mode: "host"
    tty: true
    stdin_open: true
